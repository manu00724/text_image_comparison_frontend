import { RotateCcw, X } from 'lucide-react';
import { useMemo } from 'react';
export function OriginalDocumentViewer({ html, open, onClose, onRestore }) { const safeHtml = useMemo(() => sanitizePreview(html), [html]); if (!open) return null; return <div className="fixed inset-0 z-[70] grid place-items-center bg-slate-950/50 p-4 backdrop-blur-sm" role="dialog" aria-modal="true" aria-label="Original backend document"><div className="flex max-h-[92vh] w-full max-w-5xl flex-col overflow-hidden rounded-2xl bg-white shadow-2xl"><div className="flex items-center justify-between border-b border-slate-200 px-5 py-4"><div><h2 className="font-bold text-slate-900">Original backend document</h2><p className="text-xs text-slate-500">Read-only snapshot preserved before any edits or revalidation.</p></div><button onClick={onClose} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Close original document"><X className="h-5 w-5" /></button></div><div className="min-h-0 flex-1 overflow-auto bg-slate-100 p-5"><article className="original-document mx-auto min-h-[700px] max-w-4xl bg-white p-10 shadow-sm" dangerouslySetInnerHTML={{ __html: safeHtml }} /></div><div className="flex items-center justify-between gap-3 border-t border-slate-200 px-5 py-4"><p className="text-xs text-slate-500">Restoring replaces the working draft, but keeps this snapshot available.</p><button onClick={onRestore} className="inline-flex items-center gap-2 rounded-lg bg-navy-900 px-4 py-2.5 text-xs font-bold text-white hover:bg-navy-800"><RotateCcw className="h-4 w-4" />Restore as working draft</button></div></div></div>; }
function sanitizePreview(html) { const doc = new DOMParser().parseFromString(html || '', 'text/html'); doc.querySelectorAll('script,style,iframe,object,embed').forEach((node) => node.remove()); doc.querySelectorAll('*').forEach((element) => Array.from(element.attributes).forEach((attribute) => { if (attribute.name.startsWith('on') || /javascript:/i.test(attribute.value)) element.removeAttribute(attribute.name); })); return doc.body.innerHTML; }
