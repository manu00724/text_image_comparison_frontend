import { ChevronRight, FileSearch2, ListChecks, PanelLeftClose, X } from 'lucide-react';
import { useState } from 'react';
import { ClauseList } from './ClauseList';
import { EntityList } from './EntityList';
export function Sidebar({ entities, highestClause, clauses, onSelect, open, onCollapse, mobileOpen, onMobileClose }) { const [tab, setTab] = useState('entities'); return <><div className={`fixed inset-0 z-30 bg-slate-950/30 backdrop-blur-sm transition lg:hidden ${mobileOpen ? 'opacity-100' : 'pointer-events-none opacity-0'}`} onClick={onMobileClose} /><aside className={`fixed inset-y-0 left-0 z-40 overflow-hidden border-r border-slate-200 bg-slate-50 transition-[width,transform] duration-300 lg:sticky lg:top-0 lg:h-screen lg:translate-x-0 ${mobileOpen ? 'translate-x-0' : '-translate-x-full'} ${open ? 'lg:w-[410px]' : 'lg:w-0 lg:border-r-0'}`}><div className="flex h-full w-[min(410px,92vw)] shrink-0 flex-col lg:w-[410px]"><div className="flex h-16 shrink-0 items-center justify-between border-b border-slate-200 bg-white px-4"><div className="flex items-center gap-2"><div className="rounded-lg bg-navy-900 p-2 text-white"><FileSearch2 className="h-4 w-4" /></div><div><p className="text-sm font-bold text-slate-900">Review panel</p><p className="text-[10px] uppercase tracking-wider text-slate-400">Document intelligence</p></div></div><div className="flex gap-1"><button onClick={onCollapse} className="hidden rounded-lg p-2 text-slate-500 hover:bg-slate-100 lg:block" aria-label="Collapse sidebar"><PanelLeftClose className="h-4 w-4" /></button><button onClick={onMobileClose} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100 lg:hidden" aria-label="Close sidebar"><X className="h-4 w-4" /></button></div></div><div className="grid grid-cols-2 border-b border-slate-200 bg-white px-3 pt-2"><Tab active={tab === 'entities'} onClick={() => setTab('entities')} icon={<ListChecks className="h-4 w-4" />} label="Entities" count={16} /><Tab active={tab === 'clauses'} onClick={() => setTab('clauses')} icon={<FileSearch2 className="h-4 w-4" />} label="Clauses" count={clauses.length} /></div><div className="min-h-0 flex-1 overflow-y-auto">{tab === 'entities' ? <EntityList entities={entities} highestClause={highestClause} onSelect={onSelect} /> : <ClauseList clauses={clauses} onSelect={onSelect} />}</div></div></aside>{!open && <button onClick={onCollapse} className="fixed left-0 top-24 z-20 hidden rounded-r-xl bg-navy-900 px-2 py-4 text-white shadow-lg lg:block" aria-label="Expand sidebar"><ChevronRight className="h-4 w-4" /></button>}</>; }
function Tab({ active, onClick, icon, label, count }) { return <button onClick={onClick} className={`flex items-center justify-center gap-2 border-b-2 px-3 py-3 text-xs font-semibold transition ${active ? 'border-navy-700 text-navy-800' : 'border-transparent text-slate-400 hover:text-slate-700'}`}>{icon}{label}<span className={`rounded-full px-1.5 py-0.5 text-[9px] ${active ? 'bg-navy-50 text-navy-700' : 'bg-slate-100'}`}>{count}</span></button>; }
