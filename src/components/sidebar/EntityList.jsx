import { Sparkles } from 'lucide-react';
import { entityFields } from '../../config/entityFields';
import { EntityRow } from './EntityRow';
export function EntityList({ entities, highestClause, onSelect }) { return <div className="pb-5"><div className="mx-3 mt-3 overflow-hidden rounded-xl border border-slate-200 bg-white">{entityFields.map((field) => <EntityRow key={field.key} label={field.label} value={entities[field.key]} matched={entities[field.matchKey] !== 'NO'} onSelect={onSelect} />)}</div><div className="mx-4 mt-6"><div className="text-[10px] font-bold uppercase tracking-[0.18em] text-slate-400">Derived entities</div><div className="mt-2 overflow-hidden rounded-xl border border-indigo-100 bg-white"><div className="flex items-center justify-between gap-3 px-4 py-3"><div className="flex items-center gap-2 text-xs font-semibold text-slate-500"><Sparkles className="h-4 w-4 text-indigo-500" />BG Class</div><span className="rounded-full bg-indigo-50 px-2.5 py-1 text-xs font-bold text-indigo-700">{highestClause || 'Not available'}</span></div></div></div></div>; }
